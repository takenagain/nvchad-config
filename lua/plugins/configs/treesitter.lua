local options = {
  ensure_installed = { "lua", "javascript", "typescript", "c", "rust" },
  -- sync_install = false,
  -- auto_install = true,

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
